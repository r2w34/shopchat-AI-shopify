<script>
  (function() {
    window.AIChatConfig = {
      apiUrl: {{ api_url | default: "https://shopchatai.indigenservices.com" | json }},
      shop: {{ shop.permanent_domain | json }},
      {% if customer %}
      customer: {
        email: {{ customer.email | json }},
        name: {{ customer.name | json }},
        id: {{ customer.id | json }}
      },
      {% endif %}
      primaryColor: {{ primary_color | default: "#5C6AC4" | json }},
      accentColor: {{ accent_color | default: "#00848E" | json }},
      position: {{ position | default: "bottom-right" | json }},
      welcomeMessage: {{ welcome_message | default: "Hi! How can I help you today?" | json }}
    };
    
    console.log('✅ AI Chat Widget Config Set:', window.AIChatConfig);
    
    var script = document.createElement('script');
    script.src = 'https://shopchatai.indigenservices.com/widget-loader.js';
    script.defer = true;
    document.body.appendChild(script);
  })();
</script>
